{
  "name": "@doudou/codex-app-server-protocol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Generated TypeScript bindings for codex app-server JSON-RPC protocol. Regenerate via pnpm run regen (requires codex CLI on PATH).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": ["dist", "src"],
  "scripts": {
    "regen": "rm -rf src && mkdir -p src && codex app-server generate-ts --out ./src && node ./scripts/make-index.mjs",
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "test": "echo 'no tests' && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
